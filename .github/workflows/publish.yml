name: Publish
on:
  push:
    branches:
      - master
jobs:
  lint-and-test:
    name: Test
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: build and publish
        run: |
          make ci-docker-login  
          make publish
